
const {Favourite} = require('./favourite.model');
const {User} = require('./user.model');
const {BookComment} = require('./bookComment.model');
const {BookScore} = require('./bookScore.model');


module.exports = {
    Favourite,
    User,
    BookComment,
    BookScore

}